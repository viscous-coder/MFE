// import React from "react";
// import { createMemoryHistory } from "history";
// import { MemoryRouter as BaseMemoryRouter } from "react-router-dom";

// const createMemoryRouter = (initialEntries = ["/"]) => {
//   const history = createMemoryHistory({ initialEntries });

//   const MemoryRouter = ({ children }) => {
//     return <BaseMemoryRouter history={history}>{children}</BaseMemoryRouter>;
//   };

//   return { history, MemoryRouter };
// };

// export default createMemoryRouter;
